/**
 * Switcher Module - Barrel export for all switcher components.
 */
import { SwitcherEngine } from './switcherEngine.js';
import { PreviewProgram } from './previewProgram.js';
import { TransitionEngine } from './transitionEngine.js';
import { MacroRecorder } from './macroRecorder.js';
import { MultiViewer } from './multiViewer.js';

export { SwitcherEngine, PreviewProgram, TransitionEngine, MacroRecorder, MultiViewer };
export default { SwitcherEngine, PreviewProgram, TransitionEngine, MacroRecorder, MultiViewer };
